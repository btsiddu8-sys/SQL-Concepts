# What is SQL?

SQL (Structured Query Language) is used to store, retrieve,
update, and manage data in relational databases.

## Why SQL?
- Fetch data
- Insert, update, delete records
- Create tables, views, procedures

## Example
```sql
SELECT * FROM employees;

